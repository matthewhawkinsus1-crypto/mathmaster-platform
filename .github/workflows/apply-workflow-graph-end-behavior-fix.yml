name: Apply workflow graph end-behavior fix

on:
  push:
    branches:
      - fix/workflow-graph-end-behavior

permissions:
  contents: write

jobs:
  apply-fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/workflow-graph-end-behavior
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Fix structured graph hook order
        run: python3 scripts/fix-workflow-stage-hook-order.py

      - name: Verify targeted regression
        run: node --test tests/platform/workflowGraphEndBehavior.test.mjs

      - name: Commit verified production changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/platform/workflow/WorkflowRunner.jsx
          git diff --cached --quiet && exit 0
          git commit -m "Keep structured graph branch hook-safe"
          git push origin HEAD:fix/workflow-graph-end-behavior
